#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int X, Y;
        cin >> X >> Y;

        if (100 * Y <= 107 * X)
            cout << "YES\n";
        else
            cout << "NO\n";
    }

    return 0;
}
#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, x;
        cin >> n >> x;
        if (n == x||n-x==1) {
            cout << n - x << endl;
        } else {
            cout << min(n, x) << endl;
        }
    }
    return 0;
}
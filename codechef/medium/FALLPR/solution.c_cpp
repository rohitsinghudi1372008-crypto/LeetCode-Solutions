#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	    int n;
	    cin>>n;
	    vector<long long>a(n),prefix(n),suffixMin(n);
	    for(int i=0; i<n; i++)
	    cin>>a(i);
	    prefix[0]=a[0];
	    for(int i=1; i<n; i++)
	    prefix[i]=prefix[i-1]+a[i];
	    bool good=true;
	    int firstBad=-1;
	    for(int i=0; i<n; i++)
	    
	    
	}

}
